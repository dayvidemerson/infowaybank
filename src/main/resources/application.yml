spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/infowaybank?useSSL=false
    username: root
    password: rootroot
app:
  jwtSecret: JWTSuperSecretKey
  jwtExpirationInMs: 600000